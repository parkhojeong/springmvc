slf4j - interface
logback - implementation

- level
  - trace
  - debug(dev recommended) 
  - info(default, prod recommended) 
  - warn 
  - error

log.info("info =" + name)
- + is executed and memory is allocated regardless of log level
- instead, log.info("info ={}", name)  

(application.properties) logging.level.pacakgename = LEVEL
- package and sub package level applied

@Controller annotation return value is view name
@ResponseBody annotation return value is message body

@Slf4j annotation
- log is injected